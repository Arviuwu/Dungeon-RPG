<UserControl x:Class="Dungeon_RPG.View.CharacterCreator"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dungeon_RPG.View"
             xmlns:view="clr-namespace:Dungeon_RPG.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="White">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <DataGrid Grid.Column="4" Grid.ColumnSpan="2" Grid.RowSpan="2" Grid.Row="3" ItemsSource="{Binding CharacterStore.AllCharacters, UpdateSourceTrigger=PropertyChanged}"/>
        <TextBlock Text="CharacterCreator"/>
        <Button
            Content="Menu"
            Grid.Column="5"
            Command="{Binding GoToMenu}"
            />
        <StackPanel Orientation="Horizontal"
                    Grid.Column="1" VerticalAlignment="Bottom" HorizontalAlignment="Right" Grid.ColumnSpan="2">
            <Label Content="Name"/>
            <TextBox Text="{Binding CharacterVM.Name, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"
                     Width="100"/>
        </StackPanel>
        <StackPanel 
            Grid.Column="1"
            Grid.ColumnSpan="2"
            Grid.Row="1"
            Grid.RowSpan="4">
            <view:CharacterStats DataContext="{Binding CharacterVM}"/>
        </StackPanel>
        
    </Grid>
</UserControl>
