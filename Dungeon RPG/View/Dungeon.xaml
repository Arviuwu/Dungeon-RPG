<UserControl x:Class="Dungeon_RPG.View.Dungeon"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Dungeon_RPG.View"
             xmlns:ctrl="clr-namespace:Dungeon_RPG.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             SizeChanged="UserControl_SizeChanged" d:Background="white">
    <Grid>
        <Grid.LayoutTransform >
            <ScaleTransform x:Name="MainScale"/>
        </Grid.LayoutTransform>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Button Content="Attack" Command="{Binding AttackCommand}"/>
        <Button Content="Return" Grid.Row="1" Command="{Binding ReturnCommand}"/>
        <StackPanel Grid.Column="1" Grid.ColumnSpan="2" Grid.Row="1" Grid.RowSpan="4">
            <TextBlock Text="{Binding CharacterVM.Name}"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"
                       TextAlignment="Center"
                       FontSize="30"/>
            <Image Source="{Binding CharacterVM.SpritePath}" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
            <ctrl:Healthbar DataContext="{Binding CharacterVM}"/>
            <ctrl:Manabar DataContext="{Binding CharacterVM}"/>
        </StackPanel>
        <StackPanel Grid.Column="3" Grid.ColumnSpan="2" Grid.Row="1" Grid.RowSpan="4">
            <TextBlock Text="{Binding EnemyVM.Name}"
               HorizontalAlignment="Center"
               VerticalAlignment="Center"
               TextAlignment="Center"
               FontSize="30"/>
            <Image Source="{Binding EnemyVM.SpritePath}" RenderOptions.BitmapScalingMode="NearestNeighbor"/>
            <ctrl:Healthbar DataContext="{Binding EnemyVM}"/>
            <ctrl:Manabar DataContext="{Binding EnemyVM}"/>
        </StackPanel>
    </Grid>
</UserControl>
